# UI 버튼 전수 점검 — 버그 수정·기능 개선 100

이 문서는 실제 제품 코드에 반영한 항목만 `[x]`로 표시하는 구현 장부다. 기존에 이미 동작하던 기능이나 테스트 개수는 100개에 포함하지 않는다.

## 탐색·검색·필터 (1–20)

1. [x] `Cmd/Ctrl+1~7`로 일곱 개 주요 화면을 바로 연다.
2. [x] 화면 버튼에 실제 단축키를 `aria-keyshortcuts`로 노출한다.
3. [x] 사이드바에서 위·아래 방향키로 다음 화면 버튼에 이동한다.
4. [x] 사이드바에서 Home·End로 첫/마지막 화면 버튼에 이동한다.
5. [x] `/`를 누르면 어느 화면에서든 전체 작업 검색으로 이동한다.
6. [x] 검색어가 있을 때 전용 지우기 버튼을 표시한다.
7. [x] 검색 지우기 버튼이 입력값·내부 필터·결과를 함께 초기화한다.
8. [x] 검색 입력에서 Escape로 검색어를 즉시 지운다.
9. [x] 검색 완료 후 결과 개수를 보조 기술에 알린다.
10. [x] 검색을 지운 뒤 검색 입력에 초점을 유지한다.
11. [x] ‘더 보기’ 후 새로 나타난 첫 작업 카드로 초점을 옮긴다.
12. [x] ‘더 보기’로 추가된 작업 개수를 알린다.
13. [x] 제공사 필터 칩을 방향키·Home·End로 탐색한다.
14. [x] 제공사 현황 카드를 방향키·Home·End로 탐색한다.
15. [x] 작업 폴더 목록을 위·아래 방향키·Home·End로 탐색한다.
16. [x] 정렬 변경 후 정렬 기준과 결과 개수를 알린다.
17. [x] 검색·제공사·작업 폴더·정렬을 한 번에 초기화하는 버튼을 추가한다.
18. [x] 제공사 필터 칩의 클릭 영역을 키운다.
19. [x] 검색 지우기 버튼의 터치 영역을 44px로 보강한다.
20. [x] 검색 입력에 `/` 단축키 정보를 노출한다.

## 모달·서랍·폼 접근성 (21–40)

21. [x] 대화상자가 열리면 앱 본문 전체를 `inert`로 격리한다.
22. [x] 건너뛰기 링크도 대화상자 뒤에서 초점되지 않도록 앱 셸 안으로 이동한다.
23. [x] 새 작업 모달이 열릴 때 모달 자체의 `inert`를 해제한다.
24. [x] 새 작업 모달의 `aria-hidden` 상태를 열림/닫힘과 동기화한다.
25. [x] 작업 상세 서랍이 열릴 때 배경 상호작용을 막는다.
26. [x] 작업 상세 서랍의 `aria-hidden` 상태를 열림/닫힘과 동기화한다.
27. [x] tmux 생성 모달이 열릴 때 배경 상호작용을 막는다.
28. [x] tmux 생성 모달의 `aria-hidden` 상태를 열림/닫힘과 동기화한다.
29. [x] 마지막 대화상자가 닫힌 뒤에만 앱 본문 상호작용을 복원한다.
30. [x] AI 선택기를 보조 기술용 `radiogroup`으로 변경한다.
31. [x] AI 선택 항목을 `radio`와 `aria-checked` 상태로 표현한다.
32. [x] AI 선택기에 선택 항목 한 개만 Tab 정지점으로 둔다.
33. [x] AI 선택기에서 네 방향키로 선택 항목을 바꾼다.
34. [x] AI 선택기에서 Home·End로 처음/마지막 항목을 선택한다.
35. [x] 최근 작업 폴더 버튼에 현재 선택 상태를 노출한다.
36. [x] 최근 작업 폴더를 좌우 방향키·Home·End로 탐색한다.
37. [x] 필수 입력 검증 실패를 `aria-invalid`로 표시한다.
38. [x] 잘못된 입력을 고치면 `aria-invalid`를 즉시 제거한다.
39. [x] tmux 작업 이름 규칙을 입력과 연결된 도움말로 제공한다.
40. [x] 새 작업 요청이 7,200자를 넘으면 8,000자 한도 접근 경고를 표시한다.

## 비동기 버튼·중복 실행 방지 (41–60)

41. [x] 공통 UI 비동기 버튼에 단일 실행 잠금을 추가한다.
42. [x] 처리 중인 버튼에 `aria-busy`와 시각적 진행 상태를 표시한다.
43. [x] AI 설치 문서 열기 버튼의 연속 클릭을 막는다.
44. [x] AI 설치 상태 재확인 버튼의 연속 클릭을 막는다.
45. [x] 새 작업 폴더 찾기 버튼의 연속 클릭을 막는다.
46. [x] 작업 폴더 추가 버튼의 연속 클릭을 막는다.
47. [x] 작업 폴더 제거 버튼의 연속 클릭을 막는다.
48. [x] AI 연결 상태 재확인 버튼의 연속 클릭을 막는다.
49. [x] 터미널 작업별 실행 중 상태를 추적하는 잠금 레지스트리를 추가한다.
50. [x] 같은 종류의 터미널 생성이 중복 실행되지 않게 한다.
51. [x] 같은 터미널의 재시작이 중복 실행되지 않게 한다.
52. [x] 같은 터미널의 종료가 중복 실행되지 않게 한다.
53. [x] tmux 새로고침이 중복 실행되지 않게 한다.
54. [x] 같은 tmux 칸 연결이 중복 실행되지 않게 한다.
55. [x] 같은 tmux 관리 명령이 중복 실행되지 않게 한다.
56. [x] 같은 tmux 레이아웃 변경이 중복 실행되지 않게 한다.
57. [x] tmux 생성 제출 버튼에 처리 중 잠금과 `aria-busy`를 적용한다.
58. [x] 업데이트 확인 버튼의 연속 클릭을 막고 처리 상태를 알린다.
59. [x] 업데이트 다운로드·설치 버튼의 연속 클릭을 막고 처리 상태를 알린다.
60. [x] GitHub 릴리스 열기 버튼의 연속 클릭을 막는다.

## 터미널·tmux 조작 (61–80)

61. [x] 터미널 세션 버튼에 현재 선택 상태를 노출한다.
62. [x] 터미널의 tmux 칸 버튼에 현재 선택 상태를 노출한다.
63. [x] tmux 배포판 노드에 현재 선택 상태를 노출한다.
64. [x] tmux 세션 노드에 현재 선택 상태를 노출한다.
65. [x] tmux 창 노드에 현재 선택 상태를 노출한다.
66. [x] tmux 칸 노드에 현재 선택 상태를 노출한다.
67. [x] Windows 터미널 생성 버튼을 실행 중 비활성화한다.
68. [x] Linux 터미널 생성 버튼을 실행 중 비활성화한다.
69. [x] tmux 새로고침 버튼을 실행 중 비활성화한다.
70. [x] tmux 직접 조작 연결 버튼을 실행 중 비활성화한다.
71. [x] 각 tmux 관리 버튼을 해당 명령 실행 중 비활성화한다.
72. [x] tmux 시작 폴더를 파일 선택기로 고르는 버튼을 추가한다.
73. [x] tmux 생성 실패 시 오류 메시지로 초점을 이동한다.
74. [x] tmux 이름에 공백을 입력하면 안전한 하이픈으로 정리하는 기능을 추가한다.
75. [x] tmux 필수 입력 검증 실패를 `aria-invalid`로 표시한다.
76. [x] tmux 입력을 고치면 `aria-invalid`를 즉시 제거한다.
77. [x] tmux 생성 창을 닫을 때 이전 오류 상태를 초기화한다.
78. [x] 터미널 명령 전송 중 보내기 버튼에 진행 상태를 표시한다.
79. [x] 터미널 명령 입력에 8,000자 제한과 실시간 글자 수를 표시한다.
80. [x] 터미널·tmux 세션 목록을 방향키·Home·End로 탐색한다.

## 버튼 계약·모바일·오류 복구 (81–100)

81. [x] 빈 작업 화면의 새 작업 버튼에 명시적 `type="button"`을 추가한다.
82. [x] 상세 서랍 닫기 버튼에 명시적 `type="button"`을 추가한다.
83. [x] 상세 서랍 탭 세 개에 명시적 버튼 타입을 추가한다.
84. [x] 동적으로 생성되는 작업 중단 버튼에 명시적 버튼 타입을 추가한다.
85. [x] 동적으로 생성되는 작업 재개 버튼에 명시적 버튼 타입을 추가한다.
86. [x] 아이콘 전용 버튼의 기본 클릭 영역을 40px로 키운다.
87. [x] 작업 폴더 제거 버튼의 클릭 영역을 40px로 키운다.
88. [x] 모달·서랍 닫기 버튼의 클릭 영역을 40px로 키운다.
89. [x] 터치 입력 환경의 모든 버튼·요약 컨트롤을 최소 44px로 만든다.
90. [x] 잘못된 폼 입력에 명확한 테두리와 초점 링을 표시한다.
91. [x] 처리 중인 버튼에 진행 커서와 명도 변화를 표시한다.
92. [x] 모바일 더보기 메뉴에서 방향키·Home·End로 이동한다.
93. [x] 모바일 더보기 메뉴 바깥을 누르면 메뉴를 닫는다.
94. [x] 모바일 더보기 메뉴를 닫을 때 더보기 버튼으로 초점을 돌린다.
95. [x] 상세 서랍 탭에서 위·아래 방향키도 지원한다.
96. [x] 상세 서랍 탭 목록의 가로 방향 정보를 보조 기술에 제공한다.
97. [x] 제공사 표시 설정 저장 실패 시 체크 상태와 필터를 원래대로 되돌린다.
98. [x] 새 작업 실행 실패 시 오류 메시지로 초점을 이동한다.
99. [x] 새 작업 제출 중 닫기·취소로 상태가 어긋나지 않게 보호한다.
100. [x] 모든 필터·화면 전환 결과를 하나의 상태 알림 규칙으로 통일한다.
